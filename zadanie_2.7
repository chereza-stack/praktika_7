/*Задача 7: Крутая БРС
Руководству пришла в голову идея сделать балльно-рейтинговую систему еще круче,
поэтому каждый кабинет в университете был разделен на N строк и M столбцов, 
где вход в аудиторию располагается в верхнем левом (если смотреть на карту) углу, 
а преподаватель сидит в правом нижнем.

По заданной карте и количеству баллов в клетках 
(если в клетке положительное число, то это количество баллов прибавляется к сумме баллов студента,
если отрицательное - вычитается) необходимо рассчитать маршрут, 
обеспечивающий максимальную сумму накопленных баллов при условии,
что можно двигаться только вниз и вправо (только в соседние клетки, не по диагонали).*/

#include <bits/stdc++.h>
using namespace std;

int main(){
    int n, m;
    cin >> n >> m;
    vector<vector<int>> a(n, vector<int>(m));
    for(auto &r:a) for(int &x:r) cin>>x;

    vector<vector<int>> dp(n, vector<int>(m));
    dp[0][0]=a[0][0];

    for(int i=1;i<n;i++) dp[i][0]=dp[i-1][0]+a[i][0];
    for(int j=1;j<m;j++) dp[0][j]=dp[0][j-1]+a[0][j];

    for(int i=1;i<n;i++)
        for(int j=1;j<m;j++)
            dp[i][j]=max(dp[i-1][j],dp[i][j-1])+a[i][j];

    cout << dp[n-1][m-1];
}
